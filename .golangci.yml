version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - godot
    - noctx
    - bodyclose

formatters:
  enable:
    - goimports
